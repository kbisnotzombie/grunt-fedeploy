#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-hashres/bin/grunt-hashres" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-hashres/bin/grunt-hashres" "$@"
  ret=$?
fi
exit $ret

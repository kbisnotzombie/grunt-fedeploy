#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-smushit/bin/grunt-smushit" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-smushit/bin/grunt-smushit" "$@"
  ret=$?
fi
exit $ret
